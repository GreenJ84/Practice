// JavaScript Objects (dictionaries, maps, key-value pairs) are associative arrays. Think of them as arrays indexed by strings. Objects are initialized as {}, not [ ]. Access them as object attributes (​arr.attrib​) or object indices (​arr["index"]​).

